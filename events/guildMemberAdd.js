const discord = require('discord.js');

module.exports = async (member) => {
    // const welcome = member.guild.channels.cache.get("888800181213102133");
    // welcome.send(`<@${member}>`)
    console.log(member)
};