export const reqString = {
    type: String,
    required: true
}

export const reqBool = {
    type: Boolean,
    required: true
}

export const reqArray = {
    type: Array,
    required: true
}

export const reqObject = {
    type: Object,
    required: true
}

export const optId = {
    type: Number,
    default: 0,
    required: false
}