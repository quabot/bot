export const reqString = {
	type: String,
	required: true,
};

export const reqBool = {
	type: Boolean,
	required: true,
};

export const reqNum = {
	type: Number,
	required: true,
};

export const reqArray = {
	type: Array,
	required: true,
};

export const reqObject = {
	type: Object,
	required: true,
};

export const reqAny = {
	required: true,
};