const { MessageEmbed } = require('discord.js');

module.exports = {
    name: "suggest",
    description: "Leave a suggestion.",
    async execute(client, interaction, color) {
        // import config etc.
        // show modal if all is gud
        //? hoe was t zwemmen eig
    }
}